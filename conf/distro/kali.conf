DISTRO = "Kali"
DISTRO_NAME = "Kali Yocto"
DISTRO_VERSION = "0.2"
DISTRO_CODENAME = "kali"

TARGET_VENDOR = "-kali"

LOCALCONF_VERSION = "1"
LAYER_CONF_VERSION ?= "6"

SDK_NAME = "${DISTRO}-${TCLIBC}-${SDK_ARCH}-${IMAGE_BASENAME}-${TUNE_PKGARCH}"
SDKPATH = "/opt/${DISTRO}/${SDK_VERSION}"
TCLIBCAPPEND = "" 
